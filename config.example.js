const CONFIG = {
  // Mapbox アクセストークン
  // 取得先: https://account.mapbox.com/access-tokens/
  mapboxToken: 'YOUR_MAPBOX_ACCESS_TOKEN',

  // Mapbox Studio にアップロードしたタイルセットID（任意）
  // 例: 'username.xxxxxxxx'
  // 不要な場合は空文字 '' にしてください
  tilesetId: '',

  // タイルセットのソースレイヤー名（任意）
  // Mapbox Studio の Tilesets ページで確認できます
  tilesetLayerName: '',
};
